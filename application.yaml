apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: todo-argo-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/cloudvignesh/argocd-todo-stack.git
    targetRevision: HEAD
    path: manifests
  destination: 
    server: https://kubernetes.default.svc
    namespace: workshop

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true